{% extends "base.html.twig" %}

{% block body %}
    <h2>{{ 'thanks.subtitle'|trans }}</h2>

    <p>
        {{ 'thanks.intro'|trans|raw }}
    </p>

    <a href="{{ path('form', {id: subscription.id}) }}" class="btn btn-primary">
        {{ 'thanks.button.edit'|trans }}
    </a>
{% endblock %}
