{% extends "base.html.twig" %}

{% block body %}
    <h2>{{ 'overview.update.subtitle'|trans }}</h2>

    <p>
        {{ 'overview.update.intro'|trans|raw }}
    </p>

    {% include 'subscription/partials/proplist.html.twig' %}

    <a href="{{ path('form', {id: subscription.id}) }}" class="btn btn-default">
        {{ 'overview.update.button.cancel'|trans }}
    </a>

    <a href="{{ path('confirm_update', {id: subscription.id}) }}" class="btn btn-primary">
        {{ 'overview.update.button.confirm'|trans }}
    </a>
{% endblock %}
