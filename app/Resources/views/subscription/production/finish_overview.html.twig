{% extends "base.html.twig" %}

{% block title %}{{ 'production.overview.finish.title' }}{% endblock %}
{% block body %}
    <h2>{{ 'overview.finish.subtitle'|trans }}</h2>

    <p>
        {{ 'overview.finish.intro'|trans|raw }}
    </p>

    {% include 'subscription/partials/proplist.html.twig' %}

    <a href="{{ path('connect_published_edit', {id: subscription.id}) }}" class="btn btn-default">
        {{ 'overview.finish.button.cancel'|trans }}
    </a>
    <form method="post" style="display: inline;">
        <input type="submit"
                class="btn btn-primary"
                value="{{ 'overview.finish.button.confirm'|trans }}">
    </form>

{% endblock %}
