<dl class="dl-horizontal">
    <dt>{{ 'ticketNo'|trans }}</dt>
    <dd>{{ subscription.ticketNo }}</dd>

    <dt>{{ 'contact'|trans }}</dt>
    <dd>{{ subscription.contact.firstName }} {{ subscription.contact.lastName }}
        ({{ subscription.contact.email }} / {{ subscription.contact.phone }})
    </dd>

    <dt>{{ 'metadataUrl'|trans }}</dt>
    <dd>{{ subscription.metadataUrl }}</dd>

    <dt>{{ 'logoUrl'|trans }}</dt>
    <dd>{{ subscription.logoUrl }}</dd>

    <dt>{{ 'nameEn'|trans }}</dt>
    <dd>{{ subscription.logoUrl }}</dd>

    <dt>{{ 'descriptionEn'|trans }}</dt>
    <dd>{{ subscription.logoUrl }}</dd>

    <dt>{{ 'nameNl'|trans }}</dt>
    <dd>{{ subscription.logoUrl }}</dd>

    <dt>{{ 'descriptionNl'|trans }}</dt>
    <dd>{{ subscription.logoUrl }}</dd>

    <dt>{{ 'applicationUrl'|trans }}</dt>
    <dd>{{ subscription.logoUrl }}</dd>

    <dt>{{ 'administrativeContact'|trans }}</dt>
    <dd>
        {% if subscription.administrativeContact %}
            {{ subscription.administrativeContact.firstName }} {{ subscription.administrativeContact.lastName }}
            ({{ subscription.administrativeContact.email }} / {{ subscription.administrativeContact.phone }})
        {% endif %}
    </dd>

    <dt>{{ 'technicalContact'|trans }}</dt>
    <dd>
        {% if subscription.technicalContact %}
            {{ subscription.technicalContact.firstName }} {{ subscription.technicalContact.lastName }}
            ({{ subscription.technicalContact.email }} / {{ subscription.technicalContact.phone }})
        {% endif %}
    </dd>

    <dt>{{ 'supportContact'|trans }}</dt>
    <dd>
        {% if subscription.supportContact %}
            {{ subscription.supportContact.firstName }} {{ subscription.supportContact.lastName }}
            ({{ subscription.supportContact.email }} / {{ subscription.supportContact.phone }})
        {% endif %}
    </dd>

    <dt>{{ 'givenNameAttribute'|trans }}</dt>
    <dd>
        {% if subscription.givenNameAttribute %}
            {{ subscription.givenNameAttribute.requested }} - {{ subscription.givenNameAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'surNameAttribute'|trans }}</dt>
    <dd>
        {% if subscription.surNameAttribute %}
            {{ subscription.surNameAttribute.requested }} - {{ subscription.surNameAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'commonNameAttribute'|trans }}</dt>
    <dd>
        {% if subscription.commonNameAttribute %}
            {{ subscription.commonNameAttribute.requested }} - {{ subscription.commonNameAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'displayNameAttribute'|trans }}</dt>
    <dd>{% if subscription.displayNameAttribute %}
            {{ subscription.displayNameAttribute.requested }} - {{ subscription.displayNameAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'emailAddressAttribute'|trans }}</dt>
    <dd>
        {% if subscription.givenNameAttribute %}
            {{ subscription.emailAddressAttribute.requested }} - {{ subscription.emailAddressAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'organizationAttribute'|trans }}</dt>
    <dd>
        {% if subscription.organizationAttribute %}
            {{ subscription.organizationAttribute.requested }} - {{ subscription.organizationAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'organizationTypeAttribute'|trans }}</dt>
    <dd>
        {% if subscription.organizationTypeAttribute %}
            {{ subscription.organizationTypeAttribute.requested }} - {{ subscription.organizationTypeAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'affiliationAttribute'|trans }}</dt>
    <dd>
        {% if subscription.affiliationAttribute %}
            {{ subscription.affiliationAttribute.requested }} - {{ subscription.affiliationAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'entitlementAttribute'|trans }}</dt>
    <dd>
        {% if subscription.entitlementAttribute %}
            {{ subscription.entitlementAttribute.requested }} - {{ subscription.entitlementAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'principleNameAttribute'|trans }}</dt>
    <dd>
        {% if subscription.principleNameAttribute %}
            {{ subscription.principleNameAttribute.requested }} - {{ subscription.principleNameAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'isMemberOfAttribute'|trans }}</dt>
    <dd>
        {% if subscription.isMemberOfAttribute %}
            {{ subscription.isMemberOfAttribute.requested }} - {{ subscription.isMemberOfAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'uidAttribute'|trans }}</dt>
    <dd>
        {% if subscription.uidAttribute %}
            {{ subscription.uidAttribute.requested }} - {{ subscription.uidAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'preferredLanguageAttribute'|trans }}</dt>
    <dd>
        {% if subscription.preferredLanguageAttribute %}
            {{ subscription.preferredLanguageAttribute.requested }} - {{ subscription.preferredLanguageAttribute.motivation }}
        {% endif %}
    </dd>

    <dt>{{ 'comments'|trans }}</dt>
    <dd>{{ subscription.comments }}</dd>
</dl>
